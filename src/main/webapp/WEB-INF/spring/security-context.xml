<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd">

	<!-- 검사 URL -->
	<!-- <http use-expressions="true">

		<intercept-url pattern="/login/**" access="permitAll" />
		<intercept-url pattern="/resources/**" access="permitAll" />		
		<intercept-url pattern="/admin/**" access="hasRole('ADMIN')" />
		<intercept-url pattern="/student/*" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
		
		<form-login login-page="/login/login"		
		 default-target-url="/student/student001"
			authentication-failure-url="/login/loginfail"
			
			username-parameter="id_"       
				password-parameter="pw"      
			 />
		<logout logout-url="/login/login" logout-success-url="/login/login" />

	</http>

	

	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource" id="userService"
		users-by-username-query="SELECT id_ as username, pw as password, enabled_ as enabled FROM login WHERE id_=?"
		authorities-by-username-query="SELECT id_ as username, grade AS authority FROM login WHERE id_=?" />
		</authentication-provider>
	</authentication-manager>


 -->

	<!-- provider -->
	<!-- <authentication-manager> <authentication-provider> <user-service> <user 
		name="user" password="password" authorities="ROLE_USER" /> </user-service> 
		</authentication-provider> </authentication-manager> -->

</beans:beans>